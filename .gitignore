# Generated by setuptools-scm at build time; the tag is the source of truth.
src/deblur3d_app/_version.py

__pycache__/
*.py[cod]
build/
dist/
*.egg-info/
.venv/

# Per-machine Claude Code permissions; tracking it makes every local change
# dirty the working tree, which setuptools-scm then stamps into the version.
.claude/settings.local.json
